{% for category in site.data.categories %}
    {% if page.language == "en-us" %}
        {% if category.slug == include.category %}
            {{ category.nameEnUs }}
        {% endif %}
    {% else %}
        {% if category.slug == include.category %}
            {{ category.name }}
        {% endif %}
    {% endif %}
{% endfor %}