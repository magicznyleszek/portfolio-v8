{% for category in site.data.categories %}
    {% if page.language == "en-us" %}
        {% if category.slug == include.category %}
            {{ category.name-en-us }}
        {% endif %}
    {% else %}
        {% if category.slug == include.category %}
            {{ category.name }}
        {% endif %}
    {% endif %}
{% endfor %}