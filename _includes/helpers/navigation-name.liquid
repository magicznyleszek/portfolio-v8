{% for link in site.data.navigation %}
    {% if page.language == "en-us" %}
        {% if link.slug == include.link %}
            {{ link.nameEnUs }}
        {% endif %}
    {% else %}
        {% if link.slug == include.link %}
            {{ link.name }}
        {% endif %}
    {% endif %}
{% endfor %}